# dossier-location-template

Un template latex pour vos dossier de location en France.

## Dependencies

On debian buster, run:
```bash
sudo apt install texlive-full
```

## Build

Run:
```bash
latexmk -pdf
```
